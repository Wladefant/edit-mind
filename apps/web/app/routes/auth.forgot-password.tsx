import type { MetaFunction } from "react-router";

export const meta: MetaFunction = () => {
  return [{ title: 'Reset your password | Edit Mind' }]
}
export default function ResetPassword() {
  return <div>Reset Password Page</div>;
}